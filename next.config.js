// /** @type {import('next').NextConfig} */
// const nextConfig = {
//   experimental: {
//     appDir: false,
//   },
// };

/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  // Other configurations if needed
};

module.exports = nextConfig;

